[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 140

[tool.coverage.run]
omit = [
    "src/nmbrs/exceptions/nmbrs_exceptions/*"
]
